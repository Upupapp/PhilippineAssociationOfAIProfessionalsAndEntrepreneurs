{
  "name": "paaipe-functions",
  "description": "Cloud Functions for the PAAIPE member portal (postflowit-autos, database: paaipe)",
  "type": "module",
  "engines": {
    "node": "20"
  },
  "main": "index.js",
  "scripts": {
    "serve": "firebase emulators:start --only functions",
    "deploy": "firebase deploy --only functions --project postflowit-autos"
  },
  "dependencies": {
    "firebase-admin": "^12.7.0",
    "firebase-functions": "^6.1.1"
  }
}
