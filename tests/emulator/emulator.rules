rules_version = '2';
// Placeholder loaded at emulator startup. The rules under test are injected by
// @firebase/rules-unit-testing (initializeTestEnvironment), which overrides this.
service cloud.firestore {
  match /databases/{database}/documents {
    match /{document=**} {
      allow read, write: if false;
    }
  }
}
